export var arrayStruct = {
    values:[],          //the values that the shapes represent
    states:[]            //how the shape is colored 
}//the array used everywhere

export var flags = {
    sorted:false,       //are the values sorted
    active:false,       //is sorting active at the moment
    paused:false        //is everithing paused at the moment
}//all of the flags
